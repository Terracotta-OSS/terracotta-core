rootProject.name = 'examples'


include('clustered-map')
//include('extended-server')
//include('toy')


dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        mavenCentral()
        mavenLocal()
    }
}