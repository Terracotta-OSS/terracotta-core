basic.property = hello
use.basic.property = ${basic.property}
use.system.property = ${user.home}/.tc
use.environment.variable = ${USER}
use.invalid.syntax = ${non-existant-property}
use.multiple = ${os.name} - ${java.vm.name}${java.vm.version} ${USER}
